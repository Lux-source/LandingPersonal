---
const { lang } = Astro.props;
const otherLang = lang === "es" ? "en" : "es";
---

<a
	href={`/${otherLang}`}
	class="inline-flex items-center justify-center rounded-full bg-teal-100 dark:bg-teal-800 p-1 hover:scale-125 transition-transform duration-200"
>
	{lang === "en" ? "🇪🇸" : "🇬🇧"}
</a>
