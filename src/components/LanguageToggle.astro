---
const { lang } = Astro.props;
const otherLang = lang === "es" ? "en" : "es";
---

<a
	href={`/${otherLang}`}
	class="px-3 py-1 bg-gray-200 dark:bg-gray-700 rounded-full text-lg cursor-pointer"
>
	{lang === "es" ? "🇬🇧" : "🇪🇸"}
</a>
